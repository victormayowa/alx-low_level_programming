#include <stdio.h>

/**
 * main- an entry point to the function
 * puts- prints statement
 * Return: always 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
